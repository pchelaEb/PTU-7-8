 
-->exec('C:\Users\olpch\Documents\4 КУРС 2 СЕМЕСТР\НЕЧЕТКАЯ ЛОГИКА\лр7\sg.sci', -1)
 
-->exec('C:\Users\olpch\Documents\4 КУРС 2 СЕМЕСТР\НЕЧЕТКАЯ ЛОГИКА\лр7\LR.sci', -1)
 
-->U = (0:1:100);
 
-->LC = LR(U, 20, 20, 10, sg, sg);
Внимание : переопределение функции : ls                      . Выполните funcprot(0) для отключения этого сообщения

 
-->MC = LR(U, 50, 30, 20, sg, sg);
Внимание : переопределение функции : ls                      . Выполните funcprot(0) для отключения этого сообщения

 
-->BC = LR(U, 90, 30, 10, sg, sg);
Внимание : переопределение функции : ls                      . Выполните funcprot(0) для отключения этого сообщения

-->plot(U, LC, 'b');

-->plot(U, MC, 'r');

-->plot(U, BC, 'g');

-->xgrid;

-->legend('LC', 'MC', 'BC');

-->exec('C:\Users\olpch\Documents\4 КУРС 2 СЕМЕСТР\НЕЧЕТКАЯ ЛОГИКА\лр7\c.sci', -1)

-->plot(U, MC, 'r');
 
-->xgrid

-->low_level = fsolve([30], c);
Внимание : переопределение функции : ls                      . Выполните funcprot(0) для отключения этого сообщения

Внимание : переопределение функции : ls                      . Выполните funcprot(0) для отключения этого сообщения

Внимание : переопределение функции : ls                      . Выполните funcprot(0) для отключения этого сообщения

Внимание : переопределение функции : ls                      . Выполните funcprot(0) для отключения этого сообщения

Внимание : переопределение функции : ls                      . Выполните funcprot(0) для отключения этого сообщения

Внимание : переопределение функции : ls                      . Выполните funcprot(0) для отключения этого сообщения

Внимание : переопределение функции : ls                      . Выполните funcprot(0) для отключения этого сообщения

 
-->high_level = fsolve([60], c);
Внимание : переопределение функции : ls                      . Выполните funcprot(0) для отключения этого сообщения

Внимание : переопределение функции : ls                      . Выполните funcprot(0) для отключения этого сообщения

Внимание : переопределение функции : ls                      . Выполните funcprot(0) для отключения этого сообщения

Внимание : переопределение функции : ls                      . Выполните funcprot(0) для отключения этого сообщения

Внимание : переопределение функции : ls                      . Выполните funcprot(0) для отключения этого сообщения

Внимание : переопределение функции : ls                      . Выполните funcprot(0) для отключения этого сообщения

Внимание : переопределение функции : ls                      . Выполните funcprot(0) для отключения этого сообщения

Внимание : переопределение функции : ls                      . Выполните funcprot(0) для отключения этого сообщения

Внимание : переопределение функции : ls                      . Выполните funcprot(0) для отключения этого сообщения

 
-->plot2d(low_level, 0.5, style=-9);
 
-->plot2d(high_level, 0.5, style=-9);
 
-->//pchelkina
 
-->A = [min(LC, MC)];
 
-->plot(U, A, '.b');
 
-->plot2d3(U,A,13);
 
-->xgrid; //pchelkina
 
-->numerator = 0;
 
-->denominator = 0;
 
-->for i = 1:length(U)
-->    numerator = numerator + A(i) * U(i);
-->    denominator = denominator + A(i);
-->end
 
-->mean = numerator/denominator
Внимание : переопределение функции : mean                    . Выполните funcprot(0) для отключения этого сообщения

 mean  =
 
    26.490845  
 
-->diary(0)
